# Base URL of the Eastman Colour Printers API server.
# Default (when not set) is http://localhost:3001
# Override only if the server runs on a different host or port.
VITE_API_BASE=http://localhost:3001
